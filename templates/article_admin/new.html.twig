{% extends 'content_base.html.twig' %}

{% block content_body %}
    <h1>Crate a new Article</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Create</button>
    {{ form_end(form) }}
{% endblock %}